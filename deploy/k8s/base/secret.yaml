# WARNING: This is an example only. Do NOT commit real secrets to Git.
# In production, use:
#   - SealedSecrets (Bitnami)
#   - External Secrets Operator
#   - Vault
#   - SOPS
apiVersion: v1
kind: Secret
metadata:
  name: go-api-starter-secret
  labels:
    app.kubernetes.io/name: go-api-starter
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  # These are placeholder values - replace in overlays or use external secret management
  DATABASE_URL: "postgres://user:password@postgres:5432/go_api_starter?sslmode=require"
  JWT_SECRET: "replace-with-secure-secret-in-production"
